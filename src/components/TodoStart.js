import React from "react";

function TodoStart(props) {
  return (
    <section className="todoStart">
      <button className="todoStart__button" type="submit">
        Добавить событие
      </button>
    </section>
  );
}

export default TodoStart;
